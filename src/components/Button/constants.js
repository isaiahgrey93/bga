import { colors, gradients } from 'theme';

export const buttonTypes = {
  solid: 'solid',
  outline: 'outline',
  wrapper: 'wrapper',
};

export const buttonDefaultType = buttonTypes.solid;

export const buttonColors = {
  transparent: 'transparent',
  muted: colors.grey,
  white: colors.white,
  primary: gradients.primary,
  secondary: gradients.secondary,
};

export const buttonDefaultColor = buttonColors.transparent;
